/**
 * Application identity, reported by the MCP server as `serverInfo`
 * and used in structured log entries.
 */
export const APP_INFO = {
  name: 'change-detection-store-mcp',
  version: '0.1.0',
} as const;
