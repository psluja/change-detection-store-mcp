/** Fixed page size for list_items. */
export const LIST_ITEMS_PAGE_SIZE = 100;

/** Defaults and bounds for the get_item_history `limit` parameter. */
export const HISTORY_DEFAULT_PAGE_SIZE = 50;
export const HISTORY_MIN_PAGE_SIZE = 1;
export const HISTORY_MAX_PAGE_SIZE = 200;
